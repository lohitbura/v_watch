import React from 'react'
import './Home.css';

const SuggestionButton = ({title}) => {
  return (
    <div>
        <p className='suggestion-button'>{title}</p>
    </div>


  )
}

export default SuggestionButton
